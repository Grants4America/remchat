import base64
import binascii

def system_picture():
    try:
        with open('default_picture', 'rb') as dft_pic:
            if dft_pic:
                return base64.b64encode(dft_img.read())
            else:
                return None
    except binascii.Error as e:
        print(f"Error occurred while fetching image: {e} 😒")

